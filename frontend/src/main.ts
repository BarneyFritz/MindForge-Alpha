import './app.css'

const el = document.getElementById('app')!
el.innerHTML = `<div style="padding:1rem;color:#e6e6e6;font-family:Inter,system-ui,sans-serif">
  <h1>MindForge Perplexity — Alpha V1</h1>
  <p>Frontend scaffold is up. Use API at <code>/auth/dev-login</code>, <code>/projects</code>, <code>/brainstorm</code>, <code>/export</code>.</p>
</div>`